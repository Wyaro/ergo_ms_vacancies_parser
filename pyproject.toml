[tool.poetry]
name = "vacancies_parser"
version = "0.1.0"
description = "Vacancies parsing module for ERGO MS (HeadHunter, Habr Career, SuperJob)"

[tool.poetry.dependencies]
python = ">=3.12,<3.13"
aiohttp = ">=3.9.0"

[build-system]
requires = ["poetry-core>=2.1.3"]
build-backend = "poetry.core.masonry.api"

